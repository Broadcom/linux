* Broadcom Ethernet Switch Interface

Required properties:
- compatible:	Should be "brcm,esw-ctl"
- reg:		Physical base address and length of the switch registers

Example:

esw_ctl: esw_ctl@28242000 {
	compatible = "brcm,esw-ctl";
	reg = <0x28242000 0x50>;
};
